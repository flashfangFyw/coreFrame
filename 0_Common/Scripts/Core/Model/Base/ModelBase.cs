using UnityEngine;
using System.Collections;
/*
 * 基础MODEL类包含基本 取单例和 事件派发功能
 * 
 *      使获取单例的方法变为统一的 Singleton<T>().GetInstance()方法
 *      
 */
namespace ffDevelopmentSpace
{
    public class ModelBase : EventDispatcher
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
